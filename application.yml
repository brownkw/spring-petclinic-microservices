spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/brownkw/spring-petclinic-microservices-config
        native:
          search-locations: classpath:.
  profiles:
    active: native
